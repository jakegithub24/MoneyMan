# Improvement needed in onboarding

1. Spash screen (where user accepts terms and conditions)
    - Add links of 'Terms' and 'Privacy Policy' page (keep them empty for now, pages should open github page link in new tab)
2. User profile identification (where user defines profile)
    - Change this with two screens as follows
        - Screen 1:
            - Ask if already a cloud user (cloud sync opt in): if yes, accept 'username' & 'cloud password'
        - Screen 2: If not, let user create new profile with selection from 3 profiles: Student, Gig-worker, Elderly-Person
            - Ask user if wants to setup 'local only' or 'cloud sync' account